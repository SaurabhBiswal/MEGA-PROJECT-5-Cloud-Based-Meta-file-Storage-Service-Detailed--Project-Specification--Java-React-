# ==================================================
# H2 DATABASE CONFIGURATION (Local Development)
# ==================================================

# H2 File-based Database (Persistent)
spring.datasource.url=jdbc:h2:file:./data/cloudstoragedb;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# H2 Console (Access at http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Port
server.port=8080

# Security - Temporary disable for testing
spring.security.user.name=user
spring.security.user.password=password

# ==================================================
# FILE UPLOAD CONFIGURATION
# ==================================================
file.upload-dir=uploads
spring.servlet.multipart.max-file-size=2048MB
spring.servlet.multipart.max-request-size=2048MB
spring.servlet.multipart.enabled=true

# ==================================================
# POSTGRESQL/SUPABASE CONFIGURATION (Production)
# Uncomment below when deploying to production
# ==================================================
# spring.datasource.url=jdbc:postgresql://nxvcqplomwevbkzemige.supabase.co:5432/postgres?sslmode=require
# spring.datasource.username=postgres
# spring.datasource.password=ytKgjuHhfLHYDgFo
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.datasource.hikari.connection-timeout=30000
# spring.datasource.hikari.maximum-pool-size=5

# ==================================================
# GMAIL SMTP CONFIGURATION
# ==================================================
# Note: Use a Gmail App Password (Settings > Security > 2FA > App Passwords)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=PLACEHOLDER_EMAIL
spring.mail.password=PLACEHOLDER_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Google OAuth2 Credentials
google.client-id=PLACEHOLDER_GOOGLE_CLIENT_ID
google.client-secret=PLACEHOLDER_GOOGLE_CLIENT_SECRET